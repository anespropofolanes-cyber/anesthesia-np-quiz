# 怎麼把網站放上網（給 shin-yi 的操作說明）

## 先理解一件事

這個網站是**純靜態網頁**——沒有資料庫、沒有伺服器程式。就是一堆檔案。
把 `publish/` 資料夾的內容放到任何能放網頁的地方，全世界都能用，跟你的電腦無關。

**但也因為是靜態網頁，資料是可以被拿走的。** 網站要顯示題目，就得把
`data/questions/*.json` 傳到使用者的瀏覽器。所以能用這個網站的人，
技術上就有辦法把 960 題連同解析、教材整包下載。

這不是哪個平台的缺點，是網頁的本質。真正要防，得把資料放到需要登入驗證的後端，
那就不是靜態網站了，需要另外開發也需要持續付費。

**目前的保護方式**：不公開原始碼、擋掉搜尋引擎、網址只私下給朋友。
對「讓朋友一起用」這個目的來說足夠。

---

## 第一次上傳（Netlify，約 5 分鐘）

1. 到 <https://app.netlify.com/signup> 註冊免費帳號（可用 Google 或 Email）

   > **為什麼要註冊？** 匿名拖曳的網站沒人認領會被清掉，而且每次更新都會換新網址——
   > 那表示你每改一次題庫就要重新通知所有朋友。註冊後網址固定。

2. 登入後點 **Add new site → Deploy manually**

3. 把 `publish` 這個資料夾**整個拖進去**
   （路徑：`/Users/huangxinyi/Documents/麻醉護理考題解析Fable5/publish`）

   注意是拖**資料夾本身**，不是拖裡面的檔案。

4. 等十幾秒，網址就出來了，長得像 `random-name-123456.netlify.app`

5. 想改網址：**Site configuration → Change site name**，
   例如改成 `anes-np-quiz` → 網址就變 `anes-np-quiz.netlify.app`

6. 把網址私下傳給朋友即可。**不要貼在公開的社群或論壇**——
   網址就是目前唯一的門檻。

---

## 以後要更新題庫怎麼辦

改完 `data/` 裡的內容之後：

```bash
cd "/Users/huangxinyi/Documents/麻醉護理考題解析Fable5"

# 1. 確認題庫沒問題（答案必須與官方答案卡一致）
python3 tools/validate_questions.py
python3 tools/verify_answer_keys.py --scan source/answer_keys

# 2. 重新打包
rm -rf publish && mkdir -p publish
cp index.html manifest.webmanifest sw.js robots.txt _headers publish/
cp -r css js data images icons assets publish/
rm -f publish/assets/logo-infinity-ecg.jpeg

# 3. 確認沒漏檔
python3 tools/check_site.py
```

然後到 Netlify 的 **Deploys** 分頁，把新的 `publish` 資料夾再拖進去一次。網址不變。

> **重要**：如果你改的是 `css/`、`js/` 或 `index.html`，
> 必須照 `sw.js` 開頭的說明更新版本號，否則已經用過的人會拿到舊版。
> 只改 `data/` 裡的題目內容則不需要。

---

## 擔心漏檔的時候

```bash
python3 tools/check_site.py
```

它會把每一題引用到的圖、每一份教材、每一支程式都對一遍，缺一個就報出來。
顯示「完整，沒有缺任何檔案」才表示可以上傳。

---

## 備份

**你的心血不要只放在一台電腦上。** 建議至少做一項：

- 把整個 `麻醉護理考題解析Fable5` 資料夾複製到外接硬碟或雲端硬碟
- 或之後有空時，開一個 **private（私人）** 的 GitHub 倉庫存放
  （私人倉庫不會公開原始碼，只是拿來當備份與版本紀錄，不開 Pages）

專案裡已經有 git 版本紀錄，改壞了可以還原：

```bash
git log --oneline        # 看歷史
git diff                 # 看改了什麼
git checkout -- <檔案>    # 還原某個檔案
```

---

## 使用者的筆記會怎麼樣

每個人的錯題本、書籤、筆記都存在**他自己的瀏覽器**裡，不會上傳、你也看不到。
所以：

- 你更新網站不會清掉他們的紀錄
- 但他們換手機、換瀏覽器、或清除瀏覽器資料就會不見

網站裡的「備份與匯出」頁有匯出／匯入功能，**記得提醒朋友定期匯出備份**。
