# Netlify 會讀這個檔。對所有頁面送出不索引的標頭，
# 比只靠 robots.txt 更可靠（robots.txt 只是「請求」，X-Robots-Tag 是明確指示）。
/*
  X-Robots-Tag: noindex, nofollow, noarchive, nosnippet
  Referrer-Policy: no-referrer
  X-Content-Type-Options: nosniff
